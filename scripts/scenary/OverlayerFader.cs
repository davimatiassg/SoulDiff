using Godot;
using System;

public partial class OverlayerFader : TileMapLayer
{
    
    //TODO!

    // public override void _Process(double delta)
    // {
    //     Vector2 localPlayerPos = ToLocal(PlayerController.Instance.currentBody.GlobalPosition);
    //     Vector2I playerTile = LocalToMap(localPlayerPos);

    //     bool behind = localPlayerPos.Y < GlobalPosition.Y + 0;


    //     if (GetCellSourceId(playerTile) != -1)
    //     { 

    //     }

    //     if (behind != isPlayerBehind)
    //     {
    //         isPlayerBehind = behind;
    //         Modulate = behind
    //             ? new Color(1, 1, 1, 0.4f)
    //             : new Color(1, 1, 1, 1f);
    //     }
    // }
}
